% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/excel_to_json_schema.R
\name{excel_to_json_schema}
\alias{excel_to_json_schema}
\title{Generate a JSON Schema from an Excel File}
\usage{
excel_to_json_schema(excel_file, sheet_name = 1, json_file = NULL)
}
\arguments{
\item{excel_file}{A character string specifying the path to the Excel file.}

\item{sheet_name}{A character string or numeric value specifying the sheet to read.
Defaults to \code{1} (the first sheet).}

\item{json_file}{An optional character string specifying the path to save the JSON schema file.
If \code{NULL}, the schema is returned as a string. Defaults to \code{NULL}.}
}
\value{
If \code{json_file} is \code{NULL}, returns a JSON-formatted schema string.
If \code{json_file} is provided, the schema is saved to the file, and a message is printed indicating the file location.
}
\description{
This function reads an Excel file and generates a JSON schema based on its structure.
The schema includes column names and inferred data types.
}
\examples{
\dontrun{
# Generate a JSON schema from the first sheet of "my_excel_file.xlsx"
schema <- excel_to_json_schema("my_excel_file.xlsx")
print(schema)

# Save the JSON schema to a file
excel_to_json_schema("my_excel_file.xlsx", json_file = "schema.json")
}
}
