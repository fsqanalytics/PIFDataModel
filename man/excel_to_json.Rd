% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/excel_to_json.R
\name{excel_to_json}
\alias{excel_to_json}
\title{Convert Excel Data to JSON}
\usage{
excel_to_json(excel_file, sheet_name = 1, json_file = NULL)
}
\arguments{
\item{excel_file}{A character string specifying the path to the Excel file.}

\item{sheet_name}{A character string or numeric value specifying the sheet to read.
Defaults to \code{1} (the first sheet).}

\item{json_file}{An optional character string specifying the path to save the JSON file.
If \code{NULL}, the function returns the JSON as a string. Defaults to \code{NULL}.}
}
\value{
If \code{json_file} is \code{NULL}, returns a JSON-formatted string. If \code{json_file} is provided, the JSON is saved to the file, and a message is printed indicating the file location.
}
\description{
This function reads data from an Excel file and converts it into a JSON format.
Optionally, the JSON can be saved to a file.
}
\examples{
\dontrun{
# Convert the first sheet of "my_excel_file.xlsx" and save to "output.json"
excel_to_json("my_excel_file.xlsx", json_file = "output.json")

# Convert the first sheet of "my_excel_file.xlsx" to a JSON string
json_string <- excel_to_json("my_excel_file.xlsx")
print(json_string)

# Convert the second sheet of "my_excel_file.xlsx" and save to "output2.json"
excel_to_json("my_excel_file.xlsx", sheet_name = "Sheet2", json_file = "output2.json")
}
}
