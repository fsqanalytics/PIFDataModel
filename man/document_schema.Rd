% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/document_schema.R
\name{document_schema}
\alias{document_schema}
\title{Generate or Load a JSON Schema Document}
\usage{
document_schema(r_object = NULL, save_to = NULL)
}
\arguments{
\item{r_object}{(Optional) An R object to derive a JSON schema from. If \code{NULL}, a boilerplate schema is returned.}

\item{save_to}{(Optional) File path to save the generated schema. If \code{NULL}, the schema is returned as a list.}
}
\value{
A JSON schema as a list if \code{save_to} is \code{NULL}; otherwise, saves the schema to the specified file.
}
\description{
Generates a JSON schema document dynamically based on an R object
or loads a predefined schema template from the package.
}
\examples{
\dontrun{
  # Generate a schema from an R object
  schema <- document_schema(list(id = 1, name = "Alice"))
  
  # Generate a boilerplate schema
  schema <- document_schema()
  
  # Save the schema to a file
  document_schema(list(id = 1, name = "Alice"), save_to = "user_schema.json")
}
}
